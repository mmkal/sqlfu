---
status: done
size: large
---

## New-user onboarding pass

**Status:** Complete. Two commits: task file first, then full implementation. Build green (19 pages). PR open at #42.

**Completed:** Everything in the checklist below. Root README regenerated by pre-commit hook.

**Remaining:** Nothing -- see "For the next pass" for follow-on items.

---

Redesign sqlfu's docs entry point for a developer arriving from X/Twitter. The bar: within 30 seconds they know what sqlfu is and who it's for; within 3 minutes they've seen a real end-to-end workflow; within 10 minutes they know which page to click next. Main changes:

- New "Getting Started" narrative walkthrough (`posts` app, `node:sqlite`, fully runnable)
- New "Lint Plugin" docs page
- README surgery: Quick Start becomes a pointer, Capabilities tightened, "When a migration fails" moves to migration-model
- Sidebar reorder + "Overview" label for the existing sqlfu page
- Landing page: CTA + demo button + footer updates
- `/docs` redirect updated to Getting Started

---

## Checklist

### Landing page (`website/src/pages/index.astro`)

- [x] Change primary CTA: "Read the docs" → "Get started in 3 minutes", href `/docs/sqlfu` → `/docs/getting-started` _7be5f44_
- [x] Update nav "Docs" link: `/docs/sqlfu` → `/docs/getting-started` _7be5f44_
- [x] Rename demo button: "Try the demo" → "Try in browser" (communicates no-install without extra copy) _7be5f44_
- [x] Update footer: "Static docs + studio for sqlfu. The studio at `/ui` talks to your local backend when `npx sqlfu` is running." → "The demo runs in your browser. Connect your own project with `npx sqlfu`." _7be5f44_

### `website/astro.config.mjs`

- [x] Update `/docs` redirect: `/docs/sqlfu` → `/docs/getting-started` _7be5f44_
- [x] Rename sidebar label "sqlfu" → "Overview" _7be5f44_
- [x] Add "Getting Started" entry at top of sidebar (slug: `getting-started`, link `/docs/getting-started`) _7be5f44_
- [x] Reorder sidebar: Getting Started → Overview → Adapters → Migration Model → Runtime validation → Dynamic queries → Generate examples → Observability → UI → Lint Plugin → Schema Diff Model _7be5f44_

### New page: `packages/sqlfu/docs/getting-started.md`

- [x] Filesystem diagram near top showing the final state (what files the reader will have created) _7be5f44_
- [x] Driver choice: `node:sqlite` with one-sentence note pointing to Adapters for other runtimes (Bun, Turso, D1, etc.) _7be5f44_
- [x] Narrative walkthrough, in order: init, edit definitions.sql (posts table), draft, migrate, add get-posts.sql, generate, call typed wrapper from app code _7be5f44_
- [x] "This is what success looks like" payoff: typed wrapper call, IDE hover showing inferred result type, one line about `name` field visible in observability _7be5f44_
- [x] Demo cross-link: "Open the [demo](/ui?demo=1) to see this project running in your browser -- same schema, same queries, no install." _7be5f44_
- [x] "Where to go next" section with 5 reader archetypes (Adapters, Migration Model, Runtime validation, Generate examples, UI) _7be5f44_

### New page: `packages/sqlfu/docs/lint-plugin.md`

- [x] Intro framing: "your filename is your query's identity; the lint plugin enforces it" _7be5f44_
- [x] Quick setup: `...sqlfu.configs.recommended` preset (3-line code block) _7be5f44_
- [x] Rule: `sqlfu/query-naming` -- what it flags, why, the SQL First connection _7be5f44_
- [x] Rule: `sqlfu/format-sql` -- covers inline SQL + `.sql` files via processor, `eslint --fix` autofix _7be5f44_
- [x] Manual config option and configuration options (`queriesDir`, `clientIdentifierPattern`) _7be5f44_
- [x] Reference implementation note: users can copy the preset and adjust _7be5f44_

### README surgery (`packages/sqlfu/README.md`)

- [x] Add one-liner at very top before TOC _7be5f44_
- [x] Collapse Quick Start to a pure pointer: `pnpm add sqlfu` + link to Getting Started _7be5f44_
- [x] Promote configuration fields to their own `## Configuration` reference section _7be5f44_
- [x] Move "When a migration fails" subsection to `docs/migration-model.md` _7be5f44_
- [x] Reorder Capabilities: Client, Migrator (folds Diff Engine), Type Generator, Formatter, Observability, UI, Lint Plugin, Agent Skill _7be5f44_
- [x] Tighten each Capabilities entry to 2-4 sentences + link to deep-dive page _7be5f44_
- [x] Update TOC to match new section structure and order _7be5f44_
- [x] Add to `sqlfu generate` in Command Reference: note that migrations must be applied first _7be5f44_

### `packages/sqlfu/docs/migration-model.md`

- [x] Add "When a migration fails" section moved from README (the two-outcome failure model) _7be5f44_

### New task: `tasks/generate-preflight.md`

- [x] Problem statement written, solution space noted without prescribing, status needs-grilling _7be5f44_

### Blog draft link audit (gitignored, not committed)

The blog drafts are gitignored and can only be edited in the main repo working directory, not this worktree. Flag these links for the author to fix before publishing:

- `blog/outbox.ignoreme.md` lines 88 and 170: references `sqlfu/outbox` module and links to `https://sqlfu.dev/docs/outbox` -- module does not exist yet
- `blog/introducing-sqlfu.ignoreme.md` line 110: "An outbox. ... See the [other post](https://sqlfu.dev/blog/outbox)" -- both the module and the post are pre-ship
- `blog/introducing-sqlfu-v2.ignoreme.md` line 120: same outbox bullet -- same situation
- Links that appear valid: `/docs/adapters`, `/docs/dynamic-queries`, `/ui?demo=1`

### Em-dash verification

- [x] No em-dashes in new or changed content. Pre-existing em-dashes in animation comments in `index.astro` also cleaned. `skills/using-sqlfu/SKILL.md` cleaned. _7be5f44_

### Build verification

- [x] `pnpm --filter sqlfu-website build`: 19 pages built (Astro build succeeded; `sync-ui.mjs` step requires separate UI build, pre-existing non-issue) _7be5f44_
- [x] Root `README.md` regenerated by pre-commit hook _7be5f44_

---

## Out of scope

- Outbox module: not yet implemented; will get a docs page when `sqlfu/outbox` ships
- Agent skill (`skills/using-sqlfu/SKILL.md`): has no docs URLs, no change needed
- CI enforcement of em-dash rule (manual grep only, for now)
- Formatter: surface too small for its own page; stays in README Capabilities section
- `sqlfu init`: folds into Getting Started walkthrough, not a separate destination
- `src/vendor/*/CLAUDE.md` em-dashes: agent-facing vendor notes, out of scope per prior passes

---

## For the next pass

- Em-dash grep should move into CI
- `tasks/generate-preflight.md` needs grilling before implementation
- Blog drafts can only publish after outbox ships (or outbox bullets are removed from the drafts) and the site is live
- The Authority Mismatches table in `migration-model.md` still has the two-rows-same-comparison issue flagged in the 2026-04-20 passes

---

## Implementation notes

_Grill-me interview completed 2026-04-22. 8 questions covering: landing page framing (CTA change, demo button, footer, lede/eyebrow unchanged), Getting Started page (confirmed: narrative walkthrough, posts app, `node:sqlite`, filesystem diagram, success moment with demo cross-link, 5 reader-archetype "next" section), README surgery (Option A: pure pointer Quick Start, promoted Configuration section, Capabilities reordered and tightened), sidebar order (Getting Started first, Schema Diff Model last as deep-theory page, Runtime validation moved up as early-decision feature), missing pages (lint plugin gets own page; outbox deferred since module unimplemented; formatter stays README), demo architecture (fully self-contained via sqlite-wasm, no backend needed -- posts fixture matches walkthrough scenario), generate ordering bug (separate generate-preflight task, problem statement only), final items (/docs redirect updated, SKILL.md has no docs URLs so no change, em-dash hygiene, root README pre-commit hook)._
