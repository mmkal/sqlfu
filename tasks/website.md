needs grilling!

a docs site, probably on cloudflare
also local.sqlfu.dev can have the ui hosted on the crowd so `npm install sqlfu` stays lightweight but can still effectively get you the UI - we'd need to move the UI's API to the main package, but that's fine and probably good!