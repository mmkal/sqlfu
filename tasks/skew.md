endpoint from the localhost server served by `npx sqlfu` which says which client versions work with it so we can show more useful "upgrade your sqlfu version" errors? or maybe should be the other way around, the client knows which servers it'll work with because i control that, it's on the internet. the server just reports which version it is and the client decides if it even wants to try.